# Contact form — create a key at https://resend.com after verifying the
# typefasterjoel.com domain. Without it, the form falls back to opening the
# visitor's email app (prefilled) and says so honestly.
RESEND_API_KEY=
